<div class="side-menu">
    <button id="new-chat-button">New Chat</button>
    <select id="chat-history-dropdown">
        <option value="">Select Chat History</option>
    </select>
    <select id="model-dropdown">
        <!-- Options will be populated dynamically -->
    </select>
    <div class="slider-container">
        <label for="max-tokens">Max Tokens: <span id="max-tokens-value">100</span></label>
        <input type="range" id="max-tokens" min="1" max="2048" value="100">
    </div>
    <div class="slider-container">
        <label for="temperature">Temperature: <span id="temperature-value">0.7</span></label>
        <input type="range" id="temperature" min="0" max="1" step="0.1" value="0.7">
    </div>
    <button id="clear-history">Clear History</button>
</div>